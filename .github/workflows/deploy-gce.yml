name: Deploy to GCE
on:
  push:
    branches:
      - main
jobs:
  deploy-to-gce:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3
      -
        name: List dir
        run: ls